---
# If you don't specify java_packages, the standard Ubuntu version gets installed by
# default, which is exactly what we want here.
# java_packages:
#   - openjdk-21-jdk

# Repo connection details (jenkins_stable_url, jenkins_apt_key_url) now
# live in group_vars/all/apt_repos.yml, alongside the other three
# third-party repos -- see roles/apt_repos. Nothing local left to define,
# but keeping this file (rather than dropping it from jenkins/main.yml's
# vars_files) means future jenkins-only vars have an obvious home.
{}
